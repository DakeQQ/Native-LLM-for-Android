<?xml version="1.0" encoding="utf-8"?>
<!-- Frosted-glass header: a translucent navy pane with a glossy top sheen, a luminous hairline
     border and a soft glowing line along its rounded bottom edge. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:type="linear"
                android:angle="270"
                android:startColor="@color/glassFillStrong"
                android:endColor="@color/glassFill" />
            <corners
                android:bottomLeftRadius="@dimen/ui_corner_radius"
                android:bottomRightRadius="@dimen/ui_corner_radius" />
            <stroke
                android:width="1dp"
                android:color="@color/glassStroke" />
        </shape>
    </item>
    <!-- Glossy top sheen. -->
    <item
        android:gravity="top"
        android:height="42dp"
        android:left="1dp"
        android:right="1dp"
        android:top="1dp">
        <shape android:shape="rectangle">
            <gradient
                android:type="linear"
                android:angle="270"
                android:startColor="@color/glassHighlight"
                android:endColor="@android:color/transparent" />
        </shape>
    </item>
    <!-- Bottom luminous hairline (inset to stay within the rounded corners). -->
    <item
        android:gravity="bottom"
        android:height="2dp"
        android:left="26dp"
        android:right="26dp">
        <shape android:shape="rectangle">
            <gradient
                android:type="linear"
                android:angle="0"
                android:startColor="@android:color/transparent"
                android:centerColor="@color/electricBlueGlow"
                android:endColor="@android:color/transparent" />
        </shape>
    </item>
</layer-list>


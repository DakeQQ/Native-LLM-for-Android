<?xml version="1.0" encoding="utf-8"?>
<!-- Soft energy aura behind the avatar. A single radial gradient (static) that the UI gently
     "breathes" via a tiny alpha/scale pulse — negligible cost (one small view). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <gradient
        android:type="radial"
        android:centerX="0.5"
        android:centerY="0.5"
        android:gradientRadius="29dp"
        android:startColor="@color/yellowGlow"
        android:endColor="@android:color/transparent" />
</shape>
